# SD-2003-4 — 多自由度系統之動態分析及應用

**來源：** 結構工程技師高考 · 結構動力分析與耐震設計 · 第4題
**考年：** 2003（民國92年）
**主分類：** [[SD-U1-3]] 多自由度系統之動態分析及應用
**副分類：** [[SD-U1-2]] 運動方程式推導
**分析方法：** MDOF模態分析
**標籤：** `3DOF` `剛性樓版` `勁度矩陣` `質量矩陣` `剛心偏移` `扭轉耦合` `3D框架` `激振向量` `偏心`
**驗證狀態：** ✅ verified

---

## 題幹摘要

如圖所示之**單層立體構架**：

- 樓版為**剛性**，質量為 $m$，均勻分佈
- 四根圓柱（楊氏係數 $E$，柱高 $h$）
- 截面二次矩：
  - 角柱 D、A（左側）：$2I$
  - 角柱 B、C（右側）：$I$
- 平面尺寸為 $a \times a$（正方形）

座標系（平面）：
- 原點：樓版幾何中心 = 質心（均勻質量）
- $x$ 軸：沿 DC 方向（東西）
- $y$ 軸：沿 DA 方向（南北）

各柱平面位置：

| 柱 | 座標 $(x_i, y_i)$ | 截面 $I_i$ |
|----|------------------|-----------|
| D | $(-a/2,\;-a/2)$ | $2I$ |
| A | $(-a/2,\;+a/2)$ | $2I$ |
| B | $(+a/2,\;+a/2)$ | $I$ |
| C | $(+a/2,\;-a/2)$ | $I$ |

DOF 定義（分量按 $u_1,u_2,u_3$ 排列）：
- $u_1$：$x$ 方向水平位移
- $u_2$：$y$ 方向水平位移
- $u_3$：繞垂直軸旋轉（扭轉，正向 = 逆時針）

地震：沿 **DB 對角方向**（$45°$），求 $\mathbf{K}$ 與 $\mathbf{M}$ 的**第二行係數**（$k_{12},k_{22},k_{

*（詳見 raw/solutions 完整解析）*

---

## 核心考點

**主要分類：** `SD-U1-3` 多自由度系統之動態分析及應用

**分析方法：** MDOF模態分析

**關鍵標籤：** `3DOF` `剛性樓版` `勁度矩陣` `質量矩陣` `剛心偏移` `扭轉耦合` `3D框架` `激振向量` `偏心`

---

## 解題關鍵步驟

**步驟路線：**

$$\text{各柱剛度 }k_i = 12EI_i/h^3 \;\to\; K_{ij} = \sum(\text{柱剛度貢獻}) \;\to\; M_{ij}(\text{剛體質量矩陣}) \;\to\; \mathbf{e}(\text{激振投影})$$

**關鍵陷阱：**

**陷阱 1：** 圓柱截面 → $I_x = I_y$（兩方向側向剛度相同），不需分別考慮彎曲方向。若用矩形截面則 $I_x \neq I_y$。

**陷阱 2：** 剛心偏移 → $K_{23} = K_{32} \neq 0$（$y$ 平移與扭轉耦合）。但 $K_{13} = K_{31} = 0$（$x$ 方向對稱），容易漏算或誤算符號。

**陷阱 3：** 質量矩陣是**對角矩陣**（均勻分佈質量，質心在幾何中心），$m_{12}=m_{32}=0$。

**陷阱 4：**

*（詳見完整解析）*

---

## 用到的公式

（無）

---

## 涉及陷阱

**步驟路線：**

$$\text{各柱剛度 }k_i = 12EI_i/h^3 \;\to\; K_{ij} = \sum(\text{柱剛度貢獻}) \;\to\; M_{ij}(\text{剛體質量矩陣}) \;\to\; \mathbf{e}(\text{激振投影})$$

**關鍵陷阱：**

**陷阱 1：** 圓柱截面 → $I_x = I_y$（兩方向側向剛度相同），不需分別考慮彎曲方向。若用矩形截面則 $I_x \neq I_y$。

**陷阱 2：** 剛心偏移 → $K_{23} = K_{32} \neq 0$（$y$ 平移與扭轉耦合）。但 $K_{13} = K_{31} = 0$（$x$ 方向對稱），容易漏算或誤算符號。

**陷阱 3：** 質量矩陣是**對角矩陣**（均勻分佈質量，質心在幾何中心），$m_{12}=m_{32}=0$。

**陷阱 4：**

*（詳見完整解析）*

---

## 相關題目

[[SD-2002-1]], [[SD-2002-2]], [[SD-2003-2]], [[SD-2003-3]], [[SD-2003-5]]

---

> 完整解析見 `raw/solutions/SD-2003-4/SD-2003-4.md`
> *由 compile-all 自動生成*
