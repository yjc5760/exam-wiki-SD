# SD-2003-3 — 多自由度系統之動態分析及應用

**來源：** 結構工程技師高考 · 結構動力分析與耐震設計 · 第3題
**考年：** 2003（民國92年）
**主分類：** [[SD-U1-3]] 多自由度系統之動態分析及應用
**副分類：** [[SD-U1-1]] 結構動力基本性質及原理
**分析方法：** MDOF模態分析
**標籤：** `Rayleigh阻尼` `古典阻尼` `阻尼矩陣` `振態阻尼比` `模態解耦` `比例阻尼` `MDOF`
**驗證狀態：** ✅ verified

---

## 題幹摘要

本題分三部分：

**Part A（概念）：** 一般於結構動力分析上常將阻尼矩陣假設為古典（雷利）阻尼（Classical or Rayleigh Damping），這樣的假設於計算上有何優點？

**Part B（推導）：** 於此假設下，試推導出振態阻尼比與其對應週期的關係式。

**Part C（應用）：** 應用此關係於一五個自由度系統：
- 第一振態：$T_1 = 2\,\text{s}$，$\xi_1 = 5\%$
- 第三振態：$T_3 = 1\,\text{s}$，$\xi_3 = 5\%$
- 求：第二振態 $T_2 = 1.5\,\text{s}$ 的阻尼比 $\xi_2 = ?$

（20 分）

---

---

## 核心考點

**主要分類：** `SD-U1-3` 多自由度系統之動態分析及應用

**分析方法：** MDOF模態分析

**關鍵標籤：** `Rayleigh阻尼` `古典阻尼` `阻尼矩陣` `振態阻尼比` `模態解耦` `比例阻尼` `MDOF`

---

## 解題關鍵步驟

**步驟路線：**

$$[\mathbf{C}] = \alpha[\mathbf{M}] + \beta[\mathbf{K}] \;\to\; \text{模態正交性} \;\to\; \xi_i = \frac{\alpha}{2\omega_i} + \frac{\beta\omega_i}{2} \;\to\; \text{代入已知振態} \;\to\; \text{解}\,\alpha,\beta \;\to\; \text{求}\,\xi_2$$

**關鍵陷阱：**

**陷阱 1：** Part B 推導時，模態正交性需同時用到 $\boldsymbol{\phi}_i^T\mathbf{M}\boldsymbol{\phi}_j = 0$（$i\neq j$）和 $\boldsymbol{\phi}_i^T\mathbf{K}\boldsymbol{\phi}_j =

*（詳見完整解析）*

---

## 用到的公式

（無）

---

## 涉及陷阱

**步驟路線：**

$$[\mathbf{C}] = \alpha[\mathbf{M}] + \beta[\mathbf{K}] \;\to\; \text{模態正交性} \;\to\; \xi_i = \frac{\alpha}{2\omega_i} + \frac{\beta\omega_i}{2} \;\to\; \text{代入已知振態} \;\to\; \text{解}\,\alpha,\beta \;\to\; \text{求}\,\xi_2$$

**關鍵陷阱：**

**陷阱 1：** Part B 推導時，模態正交性需同時用到 $\boldsymbol{\phi}_i^T\mathbf{M}\boldsymbol{\phi}_j = 0$（$i\neq j$）和 $\boldsymbol{\phi}_i^T\mathbf{K}\boldsymbol{\phi}_j =

*（詳見完整解析）*

---

## 相關題目

[[SD-2002-1]], [[SD-2002-2]], [[SD-2003-2]], [[SD-2003-4]], [[SD-2003-5]]

---

> 完整解析見 `raw/solutions/SD-2003-3/SD-2003-3.md`
> *由 compile-all 自動生成*
