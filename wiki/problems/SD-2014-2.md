# SD-2014-2 — 隔減震原理

**來源：** 結構工程技師高考 · 結構動力分析與耐震設計 · 第2題
**考年：** 2014（民國103年）
**主分類：** [[SD-U3-2]] 隔減震原理
**副分類：** [[SD-U2-2]] 建築耐震設計規範
**分析方法：** 隔震系統等效線性化分析（LRB 雙線性模型迭代法）
**標籤：** `基礎隔震` `LRB` `鉛心橡膠支承` `雙線性` `等效線性化` `等效勁度` `等效阻尼比` `設計位移` `迭代法` `阻尼修正係數` `等效週期` `遲滯迴圈面積` `割線勁度` `能量法`
**驗證狀態：** ✅ verified

---

## 題幹摘要

**結構設定：**

| 參數 | 說明 |
|------|------|
| 五層樓房 | 上部結構總重 $W$（假設為剛性體） |
| 柱數 | $n$ 根，每柱下方裝設**相同** LRB |
| LRB 材料模型 | 雙線性（Bilinear） |
| $k_o$ | 單支 LRB 初始勁度 |
| $F_y$ | 單支 LRB 降伏強度 |
| $\alpha k_o$ | 單支 LRB 降伏後勁度（$\alpha < 1$） |
| 地震水準 | 設計地震（回歸週期 475 年） |
| $S_{aD}$ | 5% 阻尼比設計地震加速度反應譜係數 |
| $B_S$, $B_1$ | 短週期、長週期阻尼比修正係數 |

**已知提示公式：**

$$D_D = \frac{g}{4\pi^2} \cdot \frac{S_{aD} \cdot T_{eD}^2}{B}, \quad T_{eD} = 2\pi\sqrt{\frac{W}{K_{eD} \cdot g}}, \quad \xi_{eD} = \frac{1}{2\pi}\cdot\frac{A_{TD}}{K_{eD} D_D^2}$$

**求：** 詳列計算隔震系統設計位移 $D_D$ 之分析步驟（含計算式）。（30 分）

---

---

## 核心考點

**主要分類：** `SD-U3-2` 隔減震原理

**分析方法：** 隔震系統等效線性化分析（LRB 雙線性模型迭代法）

**關鍵標籤：** `基礎隔震` `LRB` `鉛心橡膠支承` `雙線性` `等效線性化` `等效勁度` `等效阻尼比` `設計位移` `迭代法` `阻尼修正係數` `等效週期` `遲滯迴圈面積` `割線勁度` `能量法`

---

## 解題關鍵步驟

**解題路線（等效線性化迭代法）：**

```
假設初始 D_D（或初始 K_eD）
         ↓
 Step 1  建立 LRB 雙線性模型（單支 + 整體系統）
         ↓
 Step 2  計算整體有效勁度 K_eD（割線勁度）
         ↓
 Step 3  計算等效週期 T_eD
         ↓
 Step 4  計算遲滯迴圈面積 A_TD
         ↓
 Step 5  計算等效阻尼比 ξ_eD
         ↓
 Step 6  由 ξ_eD 查表得阻尼修正係數 B = B_1
         ↓
 Step 7  由反應譜計算新的 D_D
         ↓
 D_D 是否收斂？ ──否──→ 回 Step 2
         ↓ 是
   收斂 → 輸出 D_D
```

**關鍵陷阱：**

| # | 陷阱 | 應對

*（詳見完整解析）*

---

## 用到的公式

（無）

---

## 涉及陷阱

**解題路線（等效線性化迭代法）：**

```
假設初始 D_D（或初始 K_eD）
         ↓
 Step 1  建立 LRB 雙線性模型（單支 + 整體系統）
         ↓
 Step 2  計算整體有效勁度 K_eD（割線勁度）
         ↓
 Step 3  計算等效週期 T_eD
         ↓
 Step 4  計算遲滯迴圈面積 A_TD
         ↓
 Step 5  計算等效阻尼比 ξ_eD
         ↓
 Step 6  由 ξ_eD 查表得阻尼修正係數 B = B_1
         ↓
 Step 7  由反應譜計算新的 D_D
         ↓
 D_D 是否收斂？ ──否──→ 回 Step 2
         ↓ 是
   收斂 → 輸出 D_D
```

**關鍵陷阱：**

| # | 陷阱 | 應對

*（詳見完整解析）*

---

## 相關題目

[[SD-2004-2]], [[SD-2005-1]], [[SD-2006-4]], [[SD-2008-4]], [[SD-2011-1]]

---

> 完整解析見 `raw/solutions/SD-2014-2/SD-2014-2.md`
> *由 compile-all 自動生成*
